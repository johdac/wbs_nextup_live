info:
  name: searchEventsByFavorites
  type: http
  seq: 12

http:
  method: GET
  url: "{{api}}/events"
  auth: inherit

runtime:
  variables:
    - name: api
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: searchEventsByString
  type: http
  seq: 10

http:
  method: GET
  url: "{{api}}/events?search=Theo Croker"
  params:
    - name: search
      value: Theo Croker
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

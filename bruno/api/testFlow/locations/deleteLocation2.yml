info:
  name: deleteLocation2
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{api}}/locations/{{createdLocation2}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: searchEventsByGenres
  type: http
  seq: 6

http:
  method: GET
  url: "{{api}}/events?genres=rock,pop"
  params:
    - name: genres
      value: rock,pop
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

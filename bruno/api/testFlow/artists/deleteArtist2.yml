info:
  name: deleteArtist2
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{api}}/artists/{{createdArtist2}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

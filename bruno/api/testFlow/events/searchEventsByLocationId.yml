info:
  name: searchEventsByLocationId
  type: http
  seq: 13

http:
  method: GET
  url: "{{api}}/events?locationId={{createdLocation1}}"
  params:
    - name: locationId
      value: "{{createdLocation1}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

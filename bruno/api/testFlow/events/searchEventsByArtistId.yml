info:
  name: searchEventsByArtistId
  type: http
  seq: 7

http:
  method: GET
  url: "{{api}}/events?artistId={{createdArtist1}}"
  params:
    - name: artistId
      value: "{{createdArtist1}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

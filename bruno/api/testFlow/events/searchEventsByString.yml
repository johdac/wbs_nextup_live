info:
  name: searchEventsByString
  type: http
  seq: 10

http:
  method: GET
  url: "{{api}}/events?search=rot"
  params:
    - name: search
      value: rot
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

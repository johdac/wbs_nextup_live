info:
  name: updateArtist1
  type: http
  seq: 6

http:
  method: PUT
  url: "{{api}}/artists/{{createdArtist1}}"
  body:
    type: json
    data: |-
      {
        "genres": ["rock", "jazz"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

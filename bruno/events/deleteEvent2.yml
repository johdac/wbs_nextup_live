info:
  name: deleteEvent2
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{api}}/events/{{createdEvent2}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: updateArtist1
  type: http
  seq: 6

http:
  method: PUT
  url: "{{api}}/artists/{{createdArtist1}}"
  body:
    type: json
    data: |-
      {
        "genres": ["rock", "jazz"]
      }
  auth: inherit

runtime:
  variables:
    - name: createdArtist1
      value: 69a9a93a9b9722a31a20022d
    - name: api
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

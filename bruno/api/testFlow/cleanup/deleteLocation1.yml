info:
  name: deleteLocation1
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{api}}/locations/{{createdLocation1}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

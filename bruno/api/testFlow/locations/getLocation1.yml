info:
  name: getLocation1
  type: http
  seq: 4

http:
  method: GET
  url: "{{api}}/locations/{{createdLocation1}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

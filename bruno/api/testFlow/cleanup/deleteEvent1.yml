info:
  name: deleteEvent1
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{api}}/events/{{createdEvent1}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

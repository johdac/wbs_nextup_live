info:
  name: getPresignedUrl
  type: http
  seq: 1

http:
  method: POST
  url: "{{api}}/upload/presign"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
